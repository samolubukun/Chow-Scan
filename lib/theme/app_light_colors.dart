import 'package:flutter/material.dart';

class AppLightColors {
  // Brand (same across both themes)
  static const Color brand = Color(0xFF27AE60);
  static const Color brandSoft = Color(0xFF2ECC71);
  static const Color brandDim = Color(0xFF1A8C4A);
  static const Color accent = Color(0xFFE67E22);
  static const Color accentGlow = Color(0x33E67E22);

  // Backgrounds - clean light
  static const Color background = Color(0xFFF4F6F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF0F2F5);
  static const Color card = Color(0xFFFFFFFF);
  static const Color cardElevated = Color(0xFFF9FAFB);
  static const Color scaffold = Color(0xFFF4F6F8);

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);
  static const Color textOnBrand = Color(0xFFFFFFFF);

  // State
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFE67E22);
  static const Color error = Color(0xFFE74C3C);
  static const Color info = Color(0xFF3498DB);

  // Nutrient categories
  static const Color nutrientHigh = Color(0xFF27AE60);
  static const Color nutrientModerate = Color(0xFFE67E22);
  static const Color nutrientLow = Color(0xFFE74C3C);

  // Borders
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFD1D5DB);

  // Gradients
  static const List<Color> brandGradient = [Color(0xFF2ECC71), Color(0xFF27AE60)];
  static const List<Color> accentGradient = [Color(0xFFF39C12), Color(0xFFE67E22)];
  static const List<Color> surfaceGradient = [Color(0xFFFFFFFF), Color(0xFFF0F2F5)];
  static const List<Color> cardGradient = [Color(0xFFFFFFFF), Color(0xFFF9FAFB)];
}
